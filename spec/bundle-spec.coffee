# describe 'bundling support', ->
#   it '...', (done) ->
#     view = tarima('x.js.ract.jade', 'h1 {{x}}')

#     tarima([view]).render (err, result) ->
#       console.log '>>>', err, result
#       done()
