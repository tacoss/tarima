css:
  dummy: |
    return "a: hover { text-decoration: none; }"
  source: "a:hover { text-decoration: none; } "
  contain:
    - "return"

foo_css:
  source: "[hidden] { display: none }"

foo_css_css:
  source: "[hidden] { display: none }"

foo_css_css_css:
  source: "[hidden] { display: none }"

foo_css_bar:
  source: "b { font-weight: bold }"
  contain: "b "

tpl_foo_css:
  source: "b { font-weight: bold }"
  contain: "bold"

tpl_foo_css_bar:
  source: "b { font-weight: bold }"

foo_js_css:
  source: ".hidden { display: none; }"
  contain: ".hidden"

foo_css_js_css:
  source: ".foo .bar { candy: does nothing; }"
