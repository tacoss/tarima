css:
  contain:
    - function anonymous()
    - return

  fixtures:
    foo.css.css:
      label: should return css-code as is
      source: "body { color: red }"
      contain: "color: red"
      invalidate: yes

    foo.css.css.css:
      label: should return css-code as is
      source: "a { font-weight: bold }"
      missing: return
      invalidate: yes

    foo.bar.css:
      label: should return the executed css-code
      source: "body { color: red }"
      missing: function
      invalidate: yes
