jade:
  fixtures:
    # foo.html.jade:
    #   label: should return html-code from executing the jade-code
    #   source: |
    #     p.
    #       OK
    #   contain: <p>OK</p>
    #   invalidate: true

    # foo.js.jade:
    #   label: should return html-code as is
    #   source: "tag(attr, data={x:'y'})"
    #   compiled: true
    #   validate: true
