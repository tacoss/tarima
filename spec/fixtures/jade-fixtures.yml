jade:
  dummy: "function anonymous(locals){var buf = [];)}"
  source: |
    h1 I'm {{a||'?'}} template
    p: span: <%= WUT %>
    span= ok
  contain:
    - "function anonymous(locals)"
    - "var buf = [];"

foo_jade:
  source: |
    h1 I'm {{a||'?'}} template
    p: span: <%= WUT %>
    span= ok

foo_jade_jade:
  source: |
    h1 I'm {{a||'?'}} template
    p: span: <%= WUT %>
    span= ok

foo_jade_jade_jade:
  source: |
    h1 I'm {{a||'?'}} template
    p: span: <%= WUT %>
    span= ok

foo_jade_bar:
  source: |
    h1 I'm {{a||'?'}} template
    p: span: <%= WUT %>
    span= ok
  contain: "p: span:"

tpl_foo_jade:
  params:
    ok: '!?'
  source: |
    h1 I'm {{a||'?'}} template
    p: span: <%= WUT %>
    span= ok
  contain:
    - "</span>"
    - "</h1>"
    - "!?"

tpl_foo_jade_bar:
  params:
    ok: '.'
  source: |
    h1 I'm {{a||'?'}} template
    p: span: <%= WUT %>
    span= ok
  contain: "p: span:"

foo_js_jade:
  source: |
    h1= title
    p= subtitle

foo_jade_js_jade:
  source: |
    h1= title
    p= subtitle
