js:
  dummy: "function anonymous(locals_){with(locals_||{}){}}"
  source: |
    return '|{{#i}}' + n + "{{/i}}"
  contain:
    - "function anonymous(locals_)"
    - "with(locals_||{})"

foo_js:
  source: |
    return '|{{#i}}' + n + "{{/i}}"
  params:
    n: "regular javascript"

foo_js_js:
  source: |
    return '|{{#i}}' + n + "{{/i}}"
  params:
    n: "wrapped javascript"

foo_js_js_js:
  source: |
    return '|{{#i}}' + n + "{{/i}}"
  params:
    n: "wrapped javascript (don't)"

foo_js_bar:
  source: |
    return '|{{#i}}' + n + "{{/i}}"
  contain: |
    '|{{#i}}' + n + "{{/i}}"

tpl_foo_js:
  source: |
    return '|{{#i}}' + n + "{{/i}}"
  params:
    n: "evaluated javascript"

tpl_foo_js_bar:
  source: |
    return '|{{#i}}' + n + "{{/i}}"
  contain: |
    '|{{#i}}' + n + "{{/i}}"
