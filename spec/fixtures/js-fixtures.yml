js:
  contain:
    - "function anonymous(locals_)"
    - "with(locals_||{})"

  fixtures:
    foo.js.js:
      label: should return js-code as is
      source: return 2 * 3
      missing: 6
      validate: no

    foo.js.js.js:
      label: should return js-code as is
      source: return 2 + 2
      contain: 2 + 2
      validate: no

    foo.bar.js:
      label: should return the executed js-code
      source: return 2 + 2
      contain: 4
