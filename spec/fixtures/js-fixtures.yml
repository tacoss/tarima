js:
  dummy: "function anonymous(locals_){with(locals_||{}){}}"
  source: |
    return '|{{#i}}' + n + "{{/i}}"
  contain:
    - "function anonymous(locals_)"
    - "with(locals_||{})"

foo_js:
  source: |
    return '|{{#i}}' + n + "{{/i}}"

foo_js_js:
  source: |
    return '|{{#i}}' + n + "{{/i}}"

foo_js_js_js:
  source: |
    return '|{{#i}}' + n + "{{/i}}"

foo_js_bar:
  source: |
    return '|{{#i}}' + n + "{{/i}}"
  contain: " + n + "

tpl_foo_js:
  source: |
    return '|{{#i}}' + n + "{{/i}}"
  params:
    n: "evaluated javascript"

tpl_foo_js_bar:
  source: |
    return '|{{#i}}' + n + "{{/i}}"
  contain: " + n + "
