md:
  fixtures:
    foo.md.js:
      label: render() should throws an error (cannot compile js-to-md)
      source: "return '> a quote'"
      throws: true

    foo.md.css:
      label: render() should throws an error (cannot compile css-to-md)
      source: "body { color: #000 }"
      throws: true

    foo.md.html:
      label: render() should throws an error (cannot compile html-to-md)
      source: <markup>
      throws: true

    foo.md.less:
      label: render() should throws an error (cannot compile less-to-md)
      source: "strong { color: lighten(#333, 13) }"
      throws: true

    foo.md.coffee:
      label: render() should throws an error (cannot compile coffee-to-md)
      source: "-> 'string'"
      throws: true

    foo.md.ract:
      label: render() should throws an error (cannot compile ract-to-md)
      source: "<tag>{{value}}</tag>"
      throws: true

    foo.md.json:
      label: render() should throws an error (cannot compile json-to-md)
      source: "{ foo: 'bar' }"
      throws: true
