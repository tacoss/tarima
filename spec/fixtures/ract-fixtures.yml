ract:
  dummy: "return []"
  source: "<i>{{foo || 'bar'}}</i>"
  contain:
    - "return"
    - "["
    - "]"

foo_ract:
  source: "<i>{{foo || 'bar'}}</i>"

foo_ract_ract:
  source: "<i>{{foo || 'bar'}}</i>"

foo_ract_ract_ract:
  source: "<i>{{foo || 'bar'}}</i>"

foo_ract_bar:
  params:
    foo: "XD"
  source: "<i>{{foo || 'bar'}}</i>"

tpl_foo_ract:
  params:
    foo: "OK"
  source: "<i>{{foo || 'bar'}}</i>"

tpl_foo_ract_bar:
  source: "<i>{{foo || 'bar'}}</i>"

foo_js_ract:
  params:
    foo: "WUTZ"
  source: "<i>{{foo || 'bar'}}</i>"

foo_ract_js_ract:
  params:
    foo: "YEAH!"
  source: "<i>{{foo || 'bar'}}</i>"
