us:
  dummy: "function (obj){(obj = {});_.escape}"
  source: "foo {{#candy}}<%=bar||'BAR'%>{{/candy}}"
  contain:
    - "function (obj)"
    - "(obj = {});"
    - "_.escape"

foo_us:
  source: "foo {{#candy}}<%=bar||'BAR'%>{{/candy}}"

foo_us_us:
  source: "foo {{#candy}}<%=bar||'BAR'%>{{/candy}}"

foo_us_us_us:
  source: "foo {{#candy}}<%=bar||'BAR'%>{{/candy}}"

foo_us_bar:
  params:
    bar: 'fuu!'
  source: "foo {{#candy}}<%=bar||'BAR'%>{{/candy}}"
  contain: "||"

tpl_foo_us:
  params:
    bar: 'fuu!'
  source: "foo {{#candy}}<%=bar||'BAR'%>{{/candy}}"

tpl_foo_us_bar:
  params:
    bar: 'fuu!'
  source: "foo {{#candy}}<%=bar||'BAR'%>{{/candy}}"
  contain: "'BAR'"

foo_js_us:
  params:
    bar: 'fuu!'
  source: "foo {{#candy}}<%=bar||'BAR'%>{{/candy}}"

foo_us_js_us:
  params:
    bar: 'fuu!'
  source: "foo {{#candy}}<%=bar||'BAR'%>{{/candy}}"
  contain: "bar||"
